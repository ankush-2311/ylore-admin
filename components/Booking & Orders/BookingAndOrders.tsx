import CustomHeader from "@/commonComponents/Header/CustomHeader";
import { Box } from "@mui/material";

const BookingAndOrders = () => {
  return (
    <Box sx={{ padding: "0 1.5rem" }}>
      <CustomHeader title="Booking & Orders" />
    </Box>
  );
};

export default BookingAndOrders;